# GoogLeNet (Inception v1) + BiFPN + CBAM
#
# GENERATED by src/generate_necks.py -- do not edit by hand.
# Neck width 128, 2 neck pass(es).
#
# Width and pass count are shared with the matching FPN configuration,
# so the only difference between those two arms is the fusion topology.
#
# CBAM here is the open-gate variant bound in src/lib_modules.py: its gates
# start uniform and open rather than as a random mask, so appending it to a
# pretrained network does not corrupt the features on the first forward pass.
#
# Backbone is torchvision googlenet with ImageNet weights, taken through
# TVBackbone so its classifier head, and any auxiliary classifier branch,
# are removed before the children are run as a sequence.
# Pyramid levels, measured at 640 px: 480ch at index 7, 832ch at index 13, 1024ch at index 16.
# Everything after the backbone is new, which is what the warm-up phase in
# src/05_train.py exists to settle.

nc: 1

backbone:
  # [from, repeats, module, args]
  - [-1, 1, TVBackbone, [512, googlenet, 2]] # 0 backbone
  - [0, 1, Index, [480, 7]] # 1 P3/8
  - [0, 1, Index, [832, 13]] # 2 P4/16
  - [0, 1, Index, [1024, 16]] # 3 P5/32

head:
  - [1, 1, Conv, [128, 1, 1]] # 4 P3 lateral
  - [2, 1, Conv, [128, 1, 1]] # 5 P4 lateral
  - [3, 1, Conv, [128, 1, 1]] # 6 P5 lateral
  - [6, 1, nn.Upsample, [None, 2, "nearest"]] # 7
  - [[5, 7], 1, Concat, [1]] # 8
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 9
  - [-1, 1, Index, [128, 0]] # 10 P4 top-down (pass 1)
  - [10, 1, nn.Upsample, [None, 2, "nearest"]] # 11
  - [[4, 11], 1, Concat, [1]] # 12
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 13
  - [-1, 1, Index, [128, 0]] # 14 P3 out (pass 1)
  - [14, 1, Conv, [128, 3, 2]] # 15
  - [[5, 10, 15], 1, Concat, [1]] # 16
  - [-1, 1, BiFPNFuse, [384, 128, 3]] # 17
  - [-1, 1, Index, [128, 0]] # 18 P4 out (pass 1)
  - [18, 1, Conv, [128, 3, 2]] # 19
  - [[6, 19], 1, Concat, [1]] # 20
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 21
  - [-1, 1, Index, [128, 0]] # 22 P5 out (pass 1)
  - [22, 1, nn.Upsample, [None, 2, "nearest"]] # 23
  - [[18, 23], 1, Concat, [1]] # 24
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 25
  - [-1, 1, Index, [128, 0]] # 26 P4 top-down (pass 2)
  - [26, 1, nn.Upsample, [None, 2, "nearest"]] # 27
  - [[14, 27], 1, Concat, [1]] # 28
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 29
  - [-1, 1, Index, [128, 0]] # 30 P3 out (pass 2)
  - [30, 1, Conv, [128, 3, 2]] # 31
  - [[18, 26, 31], 1, Concat, [1]] # 32
  - [-1, 1, BiFPNFuse, [384, 128, 3]] # 33
  - [-1, 1, Index, [128, 0]] # 34 P4 out (pass 2)
  - [34, 1, Conv, [128, 3, 2]] # 35
  - [[22, 35], 1, Concat, [1]] # 36
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 37
  - [-1, 1, Index, [128, 0]] # 38 P5 out (pass 2)
  - [30, 1, CBAM, [128]] # 39
  - [34, 1, CBAM, [128]] # 40
  - [38, 1, CBAM, [128]] # 41
  - [[39, 40, 41], 1, Detect, [nc]] # 42

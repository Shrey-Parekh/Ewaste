# YOLO11s + BiFPN + CBAM
#
# GENERATED by src/generate_necks.py -- do not edit by hand.
# Neck width 128, 2 neck pass(es).
#
# Width and pass count are shared with the matching FPN configuration,
# so the only difference between those two arms is the fusion topology.
#
# CBAM here is the open-gate variant bound in lib_modules.py: its gates
# start uniform and open rather than as a random mask, so appending it to a
# pretrained network does not corrupt the features on the first forward pass.
#
# Replacing the neck discards its COCO weights; only the backbone, layers
# 0-10, is inherited.

nc: 1

scales:
  # [depth, width, max_channels] -- identity; channels below are literal
  s: [1.0, 1.0, 1024]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [32, 3, 2]] # 0 P1/2
  - [-1, 1, Conv, [64, 3, 2]] # 1 P2/4
  - [-1, 1, C3k2, [128, False, 0.25]] # 2
  - [-1, 1, Conv, [128, 3, 2]] # 3 P3/8
  - [-1, 1, C3k2, [256, False, 0.25]] # 4
  - [-1, 1, Conv, [256, 3, 2]] # 5 P4/16
  - [-1, 1, C3k2, [256, True]] # 6
  - [-1, 1, Conv, [512, 3, 2]] # 7 P5/32
  - [-1, 1, C3k2, [512, True]] # 8
  - [-1, 1, SPPF, [512, 5]] # 9
  - [-1, 1, C2PSA, [512]] # 10

head:
  - [4, 1, Conv, [128, 1, 1]] # 11 P3 lateral
  - [6, 1, Conv, [128, 1, 1]] # 12 P4 lateral
  - [10, 1, Conv, [128, 1, 1]] # 13 P5 lateral
  - [13, 1, nn.Upsample, [None, 2, "nearest"]] # 14
  - [[12, 14], 1, Concat, [1]] # 15
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 16
  - [-1, 1, Index, [128, 0]] # 17 P4 top-down (pass 1)
  - [17, 1, nn.Upsample, [None, 2, "nearest"]] # 18
  - [[11, 18], 1, Concat, [1]] # 19
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 20
  - [-1, 1, Index, [128, 0]] # 21 P3 out (pass 1)
  - [21, 1, Conv, [128, 3, 2]] # 22
  - [[12, 17, 22], 1, Concat, [1]] # 23
  - [-1, 1, BiFPNFuse, [384, 128, 3]] # 24
  - [-1, 1, Index, [128, 0]] # 25 P4 out (pass 1)
  - [25, 1, Conv, [128, 3, 2]] # 26
  - [[13, 26], 1, Concat, [1]] # 27
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 28
  - [-1, 1, Index, [128, 0]] # 29 P5 out (pass 1)
  - [29, 1, nn.Upsample, [None, 2, "nearest"]] # 30
  - [[25, 30], 1, Concat, [1]] # 31
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 32
  - [-1, 1, Index, [128, 0]] # 33 P4 top-down (pass 2)
  - [33, 1, nn.Upsample, [None, 2, "nearest"]] # 34
  - [[21, 34], 1, Concat, [1]] # 35
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 36
  - [-1, 1, Index, [128, 0]] # 37 P3 out (pass 2)
  - [37, 1, Conv, [128, 3, 2]] # 38
  - [[25, 33, 38], 1, Concat, [1]] # 39
  - [-1, 1, BiFPNFuse, [384, 128, 3]] # 40
  - [-1, 1, Index, [128, 0]] # 41 P4 out (pass 2)
  - [41, 1, Conv, [128, 3, 2]] # 42
  - [[29, 42], 1, Concat, [1]] # 43
  - [-1, 1, BiFPNFuse, [256, 128, 2]] # 44
  - [-1, 1, Index, [128, 0]] # 45 P5 out (pass 2)
  - [37, 1, CBAM, [128]] # 46
  - [41, 1, CBAM, [128]] # 47
  - [45, 1, CBAM, [128]] # 48
  - [[46, 47, 48], 1, Detect, [nc]] # 49
